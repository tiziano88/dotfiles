#!/bin/bash

set -x

# to install:
SOFTWARE=(
  colortest
  compton # run `compton -b` in case of issues with xmonad
#  fonts-mplus
  fonts-ubuntu-font-family-console
  keychain
#  ksnapshot
#  ntfs-3g
  parcellite
  parted
  redshift
  redshift-gtk
  rxvt-unicode
  sgt-puzzles
  terminator
  xdotool
  xfonts-terminus
  xfonts-terminus-oblique
  xmobar
#  xmonad
  xzoom
)

sudo apt-get install "${SOFTWARE[@]}"
